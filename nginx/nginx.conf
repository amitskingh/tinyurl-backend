events {}

http {
    # Load MIME types so nginx serves JS, CSS, SVG, etc. correctly
    include /etc/nginx/mime.types;

    server {
        listen 80;

        # Frontend static files location
        root /var/www/tinyurl-frontend;
        index index.html;

        # Forward socket.io requests to backend container
        location /socket.io/ {
            proxy_pass http://app:8080;

            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";

            proxy_set_header Host $host;

            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Forward API requests to backend container
        location /api/ {
            proxy_pass http://app:8080;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Forward short URL redirects to backend container
        location ~ ^/[A-Za-z0-9]+$ {
            proxy_pass http://app:8080;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Serve React/Vite SPA routes
        location / {
            try_files $uri /index.html;
        }
    }
}